name: Golang
description: Run a command in a specific golang docker container
author: https://github.com/buildkite
requirements:
  - docker
configuration:
  properties:
    version:
      type: string
    import:
      type: string
    environment:
      type: array
  required: []

